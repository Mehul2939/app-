<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /app/public/
  RewriteRule ^admin(?:/(.*))?$ /app/admin/$1 [R=302,L]
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]
  RewriteRule ^ index.html [L]
</IfModule>
